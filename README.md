# AuroraChecker Python

Python + pywebview version of AuroraChecker.

## Run

```bat
pip install -r requirements.txt
python main.py
```

If Python is not in PATH, use `run.bat`.

`run.bat` writes startup errors to `aurorachecker.log` and stays open after the app exits.
